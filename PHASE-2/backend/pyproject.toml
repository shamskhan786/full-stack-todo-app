[project]
name = "todo-backend-api"
version = "1.0.0"
description = "Todo Full-Stack Web Application - FastAPI Backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
